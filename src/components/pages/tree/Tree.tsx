import { Box } from "@mui/material";
import { TreeComponent } from "./TreeComponent";

export const Tree = () => {
  return (
    <Box sx={{ background: "#a1c4fd" }}>
      <TreeComponent />
    </Box>
  );
};
